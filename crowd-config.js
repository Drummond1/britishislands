/**
 * Optional production overrides (committed). Prefer GitHub secret CROWD_FORM_EMAIL
 * in .github/workflows/pages.yml, which updates data/crowd_suggest_config.json at deploy.
 *
 * Or uncomment and set a verified FormSubmit inbox:
 */
// window.IOB_SUGGEST_CONFIG = {
//   provider: "formsubmit",
//   formsubmitEmail: "your-verified-inbox@example.com",
// };
